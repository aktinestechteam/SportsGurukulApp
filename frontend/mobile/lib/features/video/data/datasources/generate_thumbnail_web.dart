import 'package:flutter/widgets.dart';

/// Web stub: video_player does not support frame capture on web, so
/// [generateVideoThumbnail] always returns null.
Future<({List<int> bytes, String format})?> generateVideoThumbnail(
  String videoPath, {
  Duration offset = const Duration(seconds: 1),
  BuildContext? context,
}) async {
  return null;
}
